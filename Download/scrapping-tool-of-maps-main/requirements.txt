# ============================================================
# Google Maps Lead Scraper - Requirements
# Production dependencies (pin major versions for reproducible installs)
# ============================================================

# ---- Web Framework -----------------------------------------
Flask==3.0.3
Werkzeug==3.1.3

# ---- Browser Automation (Google Maps scraping) -------------
playwright==1.51.0

# ---- HTTP Clients ------------------------------------------
requests==2.32.3
httpx[http2]>=0.27.0
urllib3>=2.2.0

# ---- HTML Parsing ------------------------------------------
# selectolax = 25x faster parsing (C/Cython Lexbor engine)
selectolax>=0.3.21
lxml>=5.2.0
beautifulsoup4==4.12.3

# ---- Fast JSON serialization (Rust-based, ~10x faster) -----
orjson>=3.10.0

# ---- System resource detection (CPU/RAM-aware concurrency) -
# Cross-platform RAM detection for the parallel worker auto-sizing.
psutil>=5.9.0

# ---- Data handling -----------------------------------------
charset-normalizer>=3.3.0

# ---- Certificate verification ------------------------------
certifi>=2024.2.2
